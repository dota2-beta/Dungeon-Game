package dev.mygame.service.internal;

/**
 * Общий интерфейс для всех классов, представляющих результат применения эффекта способностей.
 * <p>
 * Его основная цель — позволить объединять различные типы результатов применения эффектов
 * (например, {@link DamageResult}, {@link HealResult}) в одной коллекции
 * (например, {@code List<EffectResult>}) для удобства их возврата и обработки.
 * Для определения конкретного типа результата используют {@code instanceof}.
 */
public interface EffectResult {
}